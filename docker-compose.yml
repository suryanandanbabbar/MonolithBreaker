services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    volumes:
      - monolith_data:/data
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    ports:
      - "5173:80"
    depends_on:
      - backend
volumes:
  monolith_data:
